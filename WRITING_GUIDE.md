# 술술 읽히는 강의 문서 가이드 (정확함은 유지)

목표는 “가벼운 톤”이 아니라 **읽는 흐름이 끊기지 않는 구조**다. 즉, 핵심 기술 포인트(제약/트레이드오프/결정 기준)는 더 또렷하게, 문장은 더 쉽게 만든다.

## 원칙 (짧게)

- **요약 먼저, 근거는 뒤에**: 독자는 결론을 본 뒤에야 디테일을 소화한다.
- **규칙/선택 기준을 문장으로 고정**: “언제/왜 이게 정답인가?”가 1~2문장으로 나오게 한다.
- **용어는 첫 등장에만 친절하게**: 괄호로 1줄 정의 후 반복 설명은 줄인다.
- **문단은 3~5줄**: 한 문단에 한 주장만 넣고, 다음 문단으로 넘긴다.
- **정확성 우선, 과장/감탄 금지**: “무조건/항상/최고” 대신 조건을 쓴다.

## 톤 분리: Core는 매끈, Explain은 편안하게

같은 문서 안에서도 “톤”을 분리하면 읽기 쉬워진다.

- **Core(기술 핵심)**: 짧고 매끈하게. 조건/제약/결정 기준만 남긴다.
  - 예: TL;DR, Decision Rules, 비교표의 Notes, 퀴즈 정답의 첫 문장
- **Explain(설명/비유/이해 보조)**: 편안한 대화체로. 다만 사실은 정확하게.
  - 예: Why This Matters, 멘탈 모델/비유, 디브리핑 질문

권장 표기:

- Core 문장은 “규칙:” “결론:”처럼 시작해도 좋다(짧게).
- Explain 문장은 “여기서 헷갈리는 이유는…” “정리하면…” 같은 연결어로 흐름을 만든다.

## 추천 흐름(템플릿)

아래 순서를 지키면 딱딱함이 줄고, 핵심이 빨리 잡힌다.

0. **고객 사례(스토리)**: 600~1000자. 클라우드 지식이 거의 없는 개발자도 이해할 수 있게 “상황→요구→난관→전환점(AWS)→결과”로 쓴다(Explain 톤, 용어는 정확).
1. **왜 중요?**: 실제 운영에서 어디서 걸리는지(함정/비용/보안/운영)
2. **멘탈 모델**: 비유 1개 또는 “그림으로 보면 이렇다” 2~3줄
3. **결정 규칙 3개**: If/Then 형태로 3줄
3.5 **서비스 분리형 theory**: Day 안에 서비스가 여러 개면 Day 폴더 바로 아래에 `01-iam.md`, `02-sts.md`처럼 **서비스별 theory(전체 본문)**를 만들고, `00-theory-index.md`는 인덱스(링크/공통 규칙)로 둔다.
4. **헷갈리는 포인트(냄새)**: 오답 유도 포인트/레드 플래그 3~5개
5. **실습/검증**: “성공하면 무엇이 보이나”를 먼저 적고 진행
6. **마무리 TL;DR(한 줄 결론)**: 오늘의 정답/선택 기준을 1줄로 고정
7. **유사 케이스 1개**: 같은 규칙을 적용해보는 짧은 질문

## 문장 스타일 체크(빠른 자가 점검)

- 한 문장에 쉼표(,)가 2개 이상이면 둘로 쪼갠다.
- “~이다/합니다” 톤은 유지하되, **전개 연결어**를 쓴다: “그래서/정리하면/즉/반대로/주의할 점은”.
- 목록은 **동사로 시작**한다: “확인한다/비교한다/정리한다/삭제한다”.
- 표/다이어그램은 “왜 이걸 넣는지” 캡션을 1줄로 붙인다.

## Exam Guide 뱃지(shields.io) 규칙

- **짧고 이쁘게**: Domain/Task는 1개씩, 서비스는 **서비스별 뱃지로 쪼개서 3~6개**만 붙인다.
- 아이콘: 기본은 `logo=amazonwebservices`로 통일한다(서비스별 로고는 “있으면” 사용).
- `logo=`가 안 보이면 **슬러그가 틀린 것**이다: Shields의 로고 이름은 Simple Icons의 `icons/*.svg` **파일명(확장자 제외)**을 그대로 쓴다.
  - 예: AWS는 보통 `aws`가 아니라 `amazonwebservices` 계열 슬러그다(정확한 파일명은 Simple Icons 목록에서 확인).
  - 참고: https://github.com/simple-icons/simple-icons/tree/develop/icons
- 현실적으로 **AWS “서비스” 아이콘은 Simple Icons에 없는 경우가 많다** → 서비스 뱃지는 텍스트 중심 + AWS 공통 로고로 가고, 정말 필요하면 별도 이미지(로컬 SVG)로 보완한다.
- 공백은 `%20`으로 URL 인코딩한다(예: `Route%2053`, `VPC%20Endpoints`).

## Deep Dive 규칙: “타입/클래스/옵션”은 빠짐없이 표로 정리

서비스 안에 선택지가 여러 개로 갈라지는 경우(예: EC2 instance family/type, EBS volume type, S3 storage class, 구매 옵션 등)는 `## Deep Dive`에서 아래를 **반드시** 포함한다.

- **전체 목록(누락 없이)**: “이 서비스의 타입/클래스가 뭐가 있는지”를 표로 한 번에 보여준다.
- **표준/가장 많이 쓰는 선택지는 굵게**: 문서에서 추천/표준인 항목은 `**bold**`로 표시한다.
- **비용까지 같이 정리**: 각 타입/클래스에 대해 “비용 드라이버(저장비/요청비/복구비/약정/중단 등)”와 “비용 효율적인 선택 기준”을 1~2줄로 붙인다.

> 원칙: “싸다/비싸다”가 아니라 “어떤 요구 신호에서 비용 효율이 좋아지는가”로 쓴다.

## Skill(외부) 산출물에도 적용

Skill 자체 파일은 수정 불가일 수 있으니, **Skill 결과물(리포트/코칭/가이드)**에 아래 구조로 덧붙인다.

- TL;DR: 오늘 결론 1줄
- Next actions: 지금 당장 할 일 3개(명령/체크)
- Smell test: 흔한 레드 플래그 3개
- Recap: 한 문장 규칙 + 유사 케이스 질문 1개
