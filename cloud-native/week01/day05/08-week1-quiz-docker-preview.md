# 8교시 - 1주차 퀴즈와 Docker 이론 예고

## 목표

이 시간의 목표는 1주차의 핵심 개념을 점검하고, 2주차 Docker 학습으로 넘어갈 준비를 하는 것이다. Docker는 갑자기 등장하는 새 도구가 아니라, 1주차에서 본 실행 환경 문제를 줄이기 위해 등장한 도구다.

이 세션의 끝에서 우리는 다음을 할 수 있어야 한다.

- 1주차 핵심 개념을 짧은 문장으로 설명한다.
- Docker가 해결하려는 문제를 “실행 환경 차이”로 말한다.
- 이미지, 컨테이너, 레지스트리, 태그가 2주차에서 왜 필요한지 예고 수준으로 이해한다.

## 오늘 한 줄 요약

Docker는 1주차에서 본 앱 실행 조건을 포장해, 다른 환경에서도 비슷하게 실행하려는 도구다.

## 수업 이미지

![1주차 개념이 Docker 이미지와 컨테이너로 이어지는 모습](assets/stickman-docker-bridge.png)

## 오늘의 흐름

| 시간 | 단계 | 진행 |
|---|---|---|
| 17:00-17:12 | 1주차 퀴즈 | 핵심 용어를 짧게 점검한다. |
| 17:12-17:25 | Docker가 필요한 이유 | 실행 환경 차이를 다시 본다. |
| 17:25-17:38 | Docker 이론 예고 | image, container, registry, tag를 소개한다. |
| 17:38-17:47 | 2주차 준비 체크 | Docker Desktop과 안전 규칙을 확인한다. |
| 17:47-17:50 | 퇴실 티켓 | Docker에서 확인하고 싶은 질문을 적는다. |

## 1주차 퀴즈

### 1. 프로세스는 무엇인가?

정답 예:

```text
실행 중인 프로그램이다.
```

### 2. 포트는 무엇인가?

정답 예:

```text
앱이 요청을 기다리는 번호다.
```

### 3. `/health`는 왜 필요한가?

정답 예:

```text
앱이 운영 가능한 상태인지 확인하기 위해 필요하다.
```

### 4. 404와 500은 어떻게 다른가?

정답 예:

```text
404는 요청한 경로를 찾지 못한 것이고, 500은 서버가 요청 처리 중 실패한 것이다.
```

### 5. 롤백은 왜 필요한가?

정답 예:

```text
새 배포에 문제가 생겼을 때 이전 상태로 돌아가 사용자를 보호하기 위해 필요하다.
```

## Docker가 필요한 이유

Day2에서 작은 웹앱을 실행하려면 여러 조건이 필요했다.

- Python이 설치되어 있어야 한다.
- 코드 파일이 있어야 한다.
- 실행 명령을 알아야 한다.
- 포트가 맞아야 한다.
- 환경 변수가 맞아야 한다.
- 로그를 볼 수 있어야 한다.

친구 컴퓨터, 서버, 클라우드 환경으로 가면 이 조건이 달라질 수 있다. Docker는 이 실행 조건을 이미지로 포장하고, 컨테이너로 실행해서 환경 차이를 줄이려는 도구다.

## Docker 이론 핵심 예고

| 용어 | 쉬운 뜻 | 1주차와 연결 |
|---|---|---|
| Image | 실행 재료를 포장한 것 | 코드, 파일, 실행 명령 |
| Container | 이미지를 실제로 실행한 상태 | 프로세스, 포트, 로그 |
| Registry | 이미지를 보관하고 받는 곳 | GitHub가 코드 저장소라면 Registry는 이미지 저장소 |
| Tag | 이미지 버전 이름표 | 배포와 롤백에서 어떤 버전인지 구분 |
| Runtime | 컨테이너를 실제로 실행하는 담당 | 프로세스 실행 |

## VM과 Container 맛보기

Docker를 배우면 VM과 컨테이너를 비교하게 된다.

| 구분 | VM | Container |
|---|---|---|
| 비유 | 컴퓨터 한 대를 통째로 빌리는 느낌 | 앱 실행 방을 가볍게 나누는 느낌 |
| OS | 각 VM이 자기 OS를 가짐 | 보통 호스트 OS 커널을 공유 |
| 장점 | 강한 격리, 다른 OS 실행 | 빠른 시작, 가벼운 배포 |
| 주의 | 무겁고 느릴 수 있음 | 권한과 보안 설정이 중요 |

초반에는 이것만 기억한다. Docker는 가벼운 실행 환경 포장에 강하고, 컨테이너 안에서도 결국 프로세스가 돈다.

## 2주차 안전 규칙

Docker 실습에서는 삭제 명령이 나온다. 안전 규칙을 먼저 기억한다.

- `docker rm`은 컨테이너를 삭제한다.
- `docker rmi`는 이미지를 삭제한다.
- `docker system prune`은 사용하지 않는 Docker 자원을 크게 정리할 수 있으므로 혼자 실행하지 않는다.
- Linux `rm -rf /`, `rm -rf *`는 여전히 절대 실행하지 않는다.
- 명령을 실행하기 전에 지금 지우는 대상이 container인지 image인지 확인한다.

## 2주차 준비 체크

터미널에서 확인한다.

```bash
docker version
```

확인할 것:

- Client 정보가 보인다.
- Server 정보가 보인다.
- Docker Desktop이 실행 중이다.

가능하면 한 번 더 확인한다.

```bash
docker run hello-world
```

## 퇴실 티켓

아래 문장을 작성한다.

```text
1주차에서 가장 이해된 개념:
아직 헷갈리는 개념:
Docker가 해결하려는 문제를 한 문장으로 쓰면:
2주차 첫날에 꼭 확인하고 싶은 질문:
```

## 마무리

2주차부터는 Docker 명령을 더 많이 실행한다. 하지만 목표는 명령 암기가 아니다. 이미지 안에 무엇이 들어갔는지, 컨테이너가 어떤 프로세스로 실행되는지, 포트와 로그가 어떻게 보이는지 계속 확인하는 것이다.
