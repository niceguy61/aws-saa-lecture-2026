# 1주차 3일차 - Linux와 터미널 생존법

## 목표

터미널을 두려워하지 않고 Docker와 Kubernetes 명령을 따라갈 수 있게 만든다. Day2에서 작은 웹앱의 프로세스, 포트, 요청, 로그를 봤다면, Day3에서는 그 증거를 터미널로 직접 찾고 설명하는 힘을 만든다.

## 오늘 배울 내용

- 명령어 구조와 옵션 읽는 법
- 경로와 현재 작업 디렉터리
- 기본 파일 확인과 로그 읽기
- 깨진 명령어를 원인별로 고치는 방법
- 에러 메시지를 운영 질문과 incident note로 바꾸는 방법

## 난이도 기준

오전은 대부분이 따라올 수 있는 생존 기본기다. 오후는 의도적으로 난이도를 올린다. 목표는 모든 학생이 모든 문제를 완벽히 맞히는 것이 아니라, 약 70%가 증거를 모아 원인을 설명할 수 있게 만드는 것이다.

학생별 목표:

| 수준 | 오늘의 목표 |
|---|---|
| 터미널 초심자 | `pwd`, `ls`, `cd`, `cat`, `grep`을 겁내지 않고 실행한다 |
| 대학생/부트캠프 수준 | 경로, 옵션, 로그 검색으로 실패 원인을 좁힌다 |
| 주니어 엔지니어 | 설정 누락, 500 로그, incident note를 연결해 설명한다 |

## 일일 시간표

| 시간 | 교시 | 인덱스 |
|---|---|---|
| 09:00-09:50 | 1교시 | [터미널 자신감 워밍업](01-terminal-warmup.md) |
| 10:00-10:50 | 2교시 | [경로, 파일, 폴더, 현재 위치](02-path-files-working-directory.md) |
| 11:00-11:50 | 3교시 | [명령어 옵션과 help 읽기](03-command-options-help.md) |
| 12:00-12:50 | 4교시 | [로그와 에러 메시지 읽기](04-read-logs-errors.md) |
| 13:00-14:00 | 점심 | 점심시간 |
| 14:00-14:50 | 5교시 | [핸즈온: 탐색과 파일 확인 연습](05-lab-explore-files.md) |
| 15:00-15:50 | 6교시 | [미션: 깨진 명령어 고치기](06-lab-fix-broken-commands.md) |
| 16:00-16:50 | 7교시 | [에러 메시지가 말하는 것 팀별 해석](07-team-error-interpretation.md) |
| 17:00-17:50 | 8교시 | [정리와 웹앱 구조 예고](08-wrapup-webapp-preview.md) |

## 랩/미션/데모

오늘의 실습 폴더:

- [playground](playground)

실습 폴더에는 작은 서비스 구조, 설정 파일, access log, error log가 들어 있다. 일부 설정은 의도적으로 빠져 있고, 일부 명령은 일부러 깨져 있다.

## 실습 터미널 기준

오늘 자료의 명령은 Linux 계열 터미널 기준이다.

| 환경 | 권장 터미널 |
|---|---|
| Windows | WSL Ubuntu 터미널 또는 Git Bash |
| macOS | Terminal 또는 iTerm |
| Linux | 기본 Terminal |

PowerShell에서도 비슷한 일을 할 수 있지만, `ls`, `cat`, `grep`, `find`의 동작과 출력이 조금 다를 수 있다. Docker와 Kubernetes 수업에서는 Linux 기준 명령을 자주 쓰므로, Windows 학습자는 가능하면 WSL Ubuntu에서 실습한다.

## 시작 위치 확인

자료에서 “저장소 루트”라고 하면 이 저장소의 가장 위 폴더를 뜻한다. 보통 `cloud-native` 폴더가 바로 보이는 위치다.

```bash
pwd
ls
```

`ls` 결과에 `cloud-native`가 보이면 아래 명령으로 Day3 실습 폴더에 들어갈 수 있다.

```bash
cd cloud-native/week01/day03/playground
```

만약 `No such file or directory`가 나오면 명령이 틀렸다고 바로 판단하지 말고, 현재 위치가 저장소 루트인지 먼저 확인한다.

기본 시작:

```bash
cd cloud-native/week01/day03/playground
pwd
find . -maxdepth 3 -type f
```

핵심 미션:

- 파일 구조에서 API/Web 서비스 단서를 찾는다.
- access log에서 `404`, `500` 요청을 찾는다.
- error log에서 설정 누락 원인을 찾는다.
- 깨진 명령 8개를 고치고 원인을 분류한다.
- 짧은 incident note를 작성한다.

## 오늘 나오는 핵심 용어

| 용어 | 쉬운 뜻 |
|---|---|
| access log | 사용자의 요청이 들어온 기록 |
| error log | 실패나 예외 상황이 남은 기록 |
| `404` | 요청한 경로를 찾지 못함 |
| `500` | 서버가 요청 처리 중 실패함 |
| `DATABASE_URL` | 앱이 데이터베이스에 연결할 때 쓰는 설정 이름 |
| incident note | 장애나 실패 상황을 짧고 재현 가능하게 정리한 기록 |

## Cloud Native 연결

터미널 생존법은 Linux만을 위한 별도 과목이 아니다. Docker와 Kubernetes를 배우면 다음 상황을 계속 만난다.

- Docker build가 실패했는데 어느 파일을 못 찾았는지 봐야 한다.
- 컨테이너 로그에서 어떤 요청이 실패했는지 찾아야 한다.
- Kubernetes YAML을 적용했는데 현재 폴더가 달라 파일을 못 찾는다.
- Pod는 떠 있는데 설정이나 secret이 빠져 애플리케이션이 500을 낸다.

그래서 오늘은 명령어를 많이 외우기보다, 실패를 읽고 원인을 좁히는 흐름을 연습한다.

## 보충/심화 자료

- 터미널 치트시트
- 자주 보는 에러 용어집
- 심화: 표준 입력/출력과 종료 코드
