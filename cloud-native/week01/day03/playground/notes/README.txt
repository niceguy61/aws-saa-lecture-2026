Day3 terminal playground

This folder is intentionally small.
Use terminal commands to inspect files, logs, and configuration.

Useful starting commands:

pwd
ls
find . -maxdepth 3 -type f
cat config/app.env
tail -n 5 logs/access.log
grep "ERROR" logs/error.log
